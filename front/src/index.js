import React from 'react';
import ReactDOM from 'react-dom/client';
import StreamersComponent from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <StreamersComponent />
  </React.StrictMode>
);